_schema-version: "3.2"
ID: pwatest
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: pwatest-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: pwatest-app-front
    parameters:
      content-target: true
  - name: pwatest-uaa
  parameters:
    config:
      destinations:
      - name: ui5
        url: https://ui5.sap.com
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - pwaapp.zip
      name: pwaapp
      target-path: resources/
- name: pwaapp
  type: html5
  path: pwaapp
  build-parameters:
    build-result: dist/pwaapp.zip
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: pwatest-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: pwatest-xsuaa-service
    service-plan: application
- name: pwatest-app-front
  type: org.cloudfoundry.managed-service
  parameters:
    service: app-front
    service-name: pwatest-app-front-service
    service-plan: developer
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
